import styled from 'styled-components'
import { space, layout, typography, color, position } from 'styled-system'

const Text = styled.p(
  { color: '#656565', fontSize: '16px', margin: '0px' },
  space,
  layout,
  typography,
  color,
  position
)

export default Text
